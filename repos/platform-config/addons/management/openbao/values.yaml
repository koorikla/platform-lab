# Hub overrides for openbao (layered on top of the umbrella chart's values.yaml). Shape (raft PVC, static-key
# auto-unseal, self-init + configure sidecar), backup and recovery: see the chart's values.yaml header.
