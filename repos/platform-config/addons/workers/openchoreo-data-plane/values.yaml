# Fleet-wide values for openchoreo-data-plane. Nothing per cluster here (Kargo renders once per env): the plane ID
# (= cluster name = the hub ClusterDataPlane's planeID) and the agent's client cert come from the CAAPH birth kit
# (worker-birth-kit, #14) through Secret cluster-agent-tls, which the agent reads.
