# Rollout pin for env "prod". Written by Kargo promotions; edit by hand only for break-glass.
rollout:
  chartRevision: main          # git ref of the platform-charts repo (branch in the lab, chart tag once split out)
  clusters: {}                 # pin a single cluster:  prod2: { chartRevision: <ref> }
