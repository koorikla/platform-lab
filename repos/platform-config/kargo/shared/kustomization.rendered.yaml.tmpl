# Copied next to every rendered manifests.yaml (as kustomization.yaml) so Argo CD treats the folder as kustomize
# and Applications can carry inline patches. Not applied itself: excluded by the kargo-pipelines Application.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [manifests.yaml]
