apiVersion: kargo.akuity.io/v1alpha1
kind: Project
metadata:
  name: podinfo                # Kargo creates/adopts namespace "podinfo" on the hub
---
apiVersion: kargo.akuity.io/v1alpha1
kind: ProjectConfig
metadata:
  name: podinfo
  namespace: podinfo
spec:
  promotionPolicies:
    - stageSelector: { name: dev }
      autoPromotionEnabled: true
